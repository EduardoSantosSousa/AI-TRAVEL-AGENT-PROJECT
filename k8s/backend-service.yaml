apiVersion: v1
kind: Service
metadata:
  name: ai-travel-backend-service
spec:
  selector:
    app: ai-travel-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP