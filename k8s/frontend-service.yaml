apiVersion: v1
kind: Service
metadata:
  name: ai-travel-frontend-service
spec:
  selector:
    app: ai-travel-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP